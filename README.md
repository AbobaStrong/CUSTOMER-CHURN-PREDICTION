– Проведён разведочный анализ данных (EDA): проверка пропусков, дубликатов, анализ распределений числовых и категориальных признаков

– Предобработка данных: обработка пустых значений, удаление дубликатов, нормализация числовых признаков

– Визуализация распределений признаков и целевой переменной, анализ дисбаланса классов

– Кодирование категориальных признаков (One-Hot Encoding, Label Encoding)

– Построена корреляционная матрица и выявлены наиболее значимые признаки (ClientPeriod, HasContractPhone)

– Обучена логистическая регрессия с подбором гиперпараметра C через кросс-валидацию

– Достигнуто качество ROC-AUC ≈ 0.83 на валидационной выборке

– Обучен градиентный бустинг (CatBoost), выполнен GridSearch по iterations и learning_rate

– Лучший результат: ROC-AUC ≈ 0.85 (CV) и ROC-AUC ≈ 0.83 (validation)

– Сформирован файл предсказаний для тестовой выборки (submission)
